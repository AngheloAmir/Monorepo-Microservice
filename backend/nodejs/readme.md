# Node Service

This is a Node.js backend service built with TypeScript.


## Run Locally
    npm install
    npm run dev
    
The server will start at `http://localhost:8000`.

## Run with Docker
    npm run docker
    
## Test the following endpoints
    GET  /login
    POST /auth
  